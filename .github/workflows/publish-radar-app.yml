name: Publish @skyhook-io/radar-app

# Radar's source imports deep paths from @skyhook-io/k8s-ui
# (./types/*, ./utils/*, ./components/gitops/*, ./components/timeline/*,
# ./components/resources/renderers/*, ./components/checks/*, and the shared
# Input primitive shipped in k8s-ui v1.8.11). Publish a matching k8s-ui version
# FIRST before tagging a radar-app release — peer dep resolves to '>=1.8.11',
# so the imported k8s-ui source exports must already be on the registry.

on:
  push:
    tags:
      - 'radar-app-v*.*.*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v7

      - name: Get version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#radar-app-v}" >> "$GITHUB_OUTPUT"

      - name: Set package version
        run: npm version "${{ steps.version.outputs.version }}" --no-git-tag-version --allow-same-version

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Publish
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
