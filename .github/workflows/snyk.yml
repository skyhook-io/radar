name: Snyk Security

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Monday at 9am UTC
    - cron: '0 9 * * 1'

permissions:
  contents: read
  security-events: write

jobs:
  snyk-go:
    name: Snyk Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.22'

      - name: Set up Snyk
        uses: snyk/actions/setup@master

      - name: Snyk test
        run: snyk test --severity-threshold=high --org=skyhook
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        continue-on-error: true

      - name: Snyk code test
        id: snyk-code
        run: snyk code test --sarif-file-output=snyk-code.sarif --org=skyhook
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        continue-on-error: true

      - name: Upload Snyk Code results to GitHub
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: snyk-code.sarif
        if: always() && steps.snyk-code.outcome == 'success'

  snyk-node:
    name: Snyk Node
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Set up Snyk
        uses: snyk/actions/setup@master

      - name: Snyk test
        run: snyk test --severity-threshold=high --org=skyhook
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        working-directory: web
        continue-on-error: true

      - name: Snyk monitor
        run: snyk monitor --org=skyhook
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        working-directory: web
        if: github.ref == 'refs/heads/main'
