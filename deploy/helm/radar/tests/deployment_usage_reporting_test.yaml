suite: deployment usage data env
templates:
  - deployment.yaml
tests:
  - it: leaves the choice to the team by default
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: RADAR_USAGE_REPORTING
            value: "on"
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: RADAR_USAGE_REPORTING
            value: "off"
  - it: turns usage data on for everyone
    set:
      usageReporting.enabled: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: RADAR_USAGE_REPORTING
            value: "on"
  - it: keeps usage data off for everyone
    set:
      usageReporting.enabled: false
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: RADAR_USAGE_REPORTING
            value: "off"
  - it: never sets usage data under Radar Cloud
    set:
      usageReporting.enabled: true
      cloud.enabled: true
      cloud.token: x
      cloud.clusterName: x
      cloud.url: wss://hub.example/agent
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: RADAR_USAGE_REPORTING
            value: "on"
